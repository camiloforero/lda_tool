import { Meteor } from 'meteor/meteor';
import '/imports/api/server/ldm_stats.js';
import '/imports/api/server/expa_client.js';
import '/imports/api/server/expa_methods.js';

Meteor.startup(() => {
  // code to run on server at startup
});
